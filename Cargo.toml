[package]
name = "harder-better-faster-stronger-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
hashbrown = "0.16.1"
libc = "0.2.182"

[profile.release]
lto = "fat"
panic = "abort"
debug = true
